package org.dataconservancy.packaging.tool.model.dprofile;

/**
 * Property supplied by the system to a domain object.
 */
public enum SuppliedProperty {
    FILE_NAME, FILE_SIZE, FILE_FORMAT_URI, FILE_CREATED_DATE, FILE_MODIFIED_DATE;
}
